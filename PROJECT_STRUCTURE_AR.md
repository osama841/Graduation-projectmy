# 📂 الدليل الشامل لهيكلية ملفات المشروع (Full Project Structure Breakdown)

دليل تفصيلي لكل مجلد وملف في مشروع **SecureGate DRM**، مع شرح وظيفة كل ملف.

---

```
Graduation Project (Main Folder)
│
├── 📄 SecureGate_DRM_Solution.sln       # ملف السوليوشن (يفتح المشروع كله في Visual Studio)
│
├── � DRM.Security.Native               # [C++] المكون الأمني (صندوق الحماية الأسود)
│   ├── � CMakeLists.txt                # ملف بناء المشروع للصيغة الأصلية (C++)
│   ├── � framework.h                   # ملفات الترويسة الأساسية للويندوز
│   ├── � pch.h / pch.cpp               # Precompiled Header (تسريع البناء)
│   ├── 📄 dllmain.cpp                   # نقطة دخول مكتبة الـ DLL
│   ├── � EncryptionEngine.cpp          # 🔐 كود التشفير الأساسي (AES-256 Core)
│   ├── � FingerprintGen.cpp            # 🆔 كود توليد بصمة الجهاز (Hardware ID Generation)
│   └── 📄 AntiDebugger.cpp              # 🛡️ كود كشف برامج التهكير والتنقيح
│
├── � src                               # [C#] الكود المصدري للتطبيقات
│   │
│   ├── 📂 Apps.AdminPanel               # 🎮 لوحة تحكم المسؤول (لتشفير الفيديو)
│   │   ├── 📄 App.xaml                  # تعريف التطبيق وموارده العامة
│   │   ├── 📄 EncryptionPage.xaml       # واجهة المستخدم لاختيار وتشفير الملفات
│   │   ├── 📄 MainWindow.xaml           # النافذة الرئيسية للوحة
│   │   ├── 📂 Services                  # الخدمات المنطقية
│   │   │   └── 📄 EncryptionService.cs  # ⚙️ كود التشفير الذي يستدعي المكتبة (تم التعديل عليه)
│   │   ├── 📂 Models                    # نماذج البيانات
│   │   │   └── 📄 DrmFile.cs            # تعريف شكل كائن "الملف" (الاسم، المسار، الحجم)
│   │   └── 📂 Assets                    # الأيقونات والصور
│   │
│   ├── 📂 SecurePlayer                  # 🎬 تطبيق المشغل الآمن (للمستخدم النهائي)
│   │   ├── 📄 App.xaml                  # تعريف التطبيق
│   │   ├── 📄 MainWindow.xaml           # الواجهة الرئيسية المعربة (القائمة الجانبية)
│   │   ├── 📂 Pages                     # صفحات التطبيق
│   │   │   └── 📄 VideoPlayerPage.xaml  # شاشة عرض الفيديو وأزرار التحكم (Play/Pause)
│   │   ├── 📂 Services                  # الخدمات
│   │   │   ├── 📄 DecryptionService.cs  # 🔓 كود فك التشفير (الذي يكلم المحرك)
│   │   │   └── 📄 FileService.cs        # إدارة فتح الملفات
│   │   └── 📂 Styles                    # ملفات التصميم (Dark Theme)
│   │
│   └── 📂 DRM.Shared                    # 🔗 المكتبة الوسيطة (الجسر)
│       ├── 📄 Class1.cs                 # ملف افتراضي (يمكن حذفه)
│       ├── 📂 NativeWrappers            # أغلفة الاتصال بـ C++
│       │   ├── 📄 NativeCallHelper.cs   # مساعد لاستدعاء دوال الـ DLL بأمان
│       │   └── 📄 HardwareID_Wrapper.cs # غلاف C# لجلب بصمة الجهاز من C++
│       └── 📂 Security                  #
│           └── 📄 SecureVideoEngine.cs  # ⚙️ المحرك الرئيسي في C# الذي يكلم EncryptionEngine.cpp
│
└── 📂 tests                             # اختبارات الكود (للتأكد أن الدوال تعمل)
    └── 📄 UnitTest1.cs                  # ملف اختبار بسيط
```

---

## 🛠️ كيف يعمل النظام (Workflow) بالتفصيل

### 1. مرحلة الحماية (AdminPanel)
1.  يختار المسؤول ملف فيديو عبر **`EncryptionPage.xaml`**.
2.  يُرسل الملف إلى **`EncryptionService.cs`**.
3.  تقوم الخدمة بتوليد مفتاح عشوائي عبر **`SecureVideoEngine.cs`** (في Shared).
4.  يقوم **`SecureVideoEngine`** باستدعاء **`EncryptionEngine.cpp`** (في Native) لتشفير البيانات فعلياً.
5.  النتيجة: ملف `.enc` مشفر + ملف `.key`.

### 2. مرحلة التشغيل (SecurePlayer)
1.  يفتح المستخدم ملف `.enc` في **`VideoPlayerPage.xaml`**.
2.  يقرأ الكود ملف `.key` ويستخرج التوقيع.
3.  يتم تمرير الملف والمفتاح إلى **`DecryptionService.cs`**.
4.  تستدعي الخدمة **`SecureVideoEngine.cs`** مرة أخرى لفك التشفير.
5.  يتم حفظ نسخة مؤقتة وتشغيلها للمستخدم.

---

## 🌟 أهم التعديلات الأخيرة التي قمنا بها
1.  **`EncryptionService.cs`**: حولناه من مجرد "واجهة عرض" إلى أداة تشفير حقيقية.
2.  **`DecryptionService.cs`**: أضفنا له القدرة على فك التشفير وحفظ الملفات.
3.  **`SecureVideoEngine.cs`**: أصلحنا مشكلة استدعاء `GenerateRandomBytes`.
4.  **`MainWindow.xaml` & `VideoPlayerPage.xaml`**: عربنا الواجهات بالكامل.
