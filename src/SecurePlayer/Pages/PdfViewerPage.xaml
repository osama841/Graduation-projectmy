<Page x:Class="SecurePlayer.Pages.PdfViewerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
      Background="#0D0E1A">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Enhanced Header -->
        <Grid Grid.Row="0" Margin="0,0,0,30">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <Border CornerRadius="12" Padding="14,10" Margin="0,0,18,0">
                    <Border.Background>
                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                            <GradientStop Color="#1F2139" Offset="0"/>
                            <GradientStop Color="#252840" Offset="1"/>
                        </LinearGradientBrush>
                    </Border.Background>
                    <Border.Effect>
                        <DropShadowEffect Color="#6366F1" BlurRadius="12" ShadowDepth="0" Opacity="0.4"/>
                    </Border.Effect>
                    <TextBlock Text="📄" FontSize="22" Foreground="#6366F1"/>
                </Border>
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="PDF Viewer" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,3">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#6366F1" BlurRadius="10" ShadowDepth="0" Opacity="0.3"/>
                        </TextBlock.Effect>
                    </TextBlock>
                    <TextBlock Text="View protected documents securely" FontSize="13" Foreground="#A1A8B8" FontWeight="Light"/>
                </StackPanel>
            </StackPanel>

            <!-- Open File Button -->
            <Button Click="OpenFile_Click" HorizontalAlignment="Right" VerticalAlignment="Center">
                <Button.Content>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="📂" FontSize="16" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="Open PDF" FontSize="14" FontWeight="SemiBold" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button.Content>
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Background" Value="#5E60CE"/>
                        <Setter Property="Foreground" Value="White"/>
                        <Setter Property="Padding" Value="18,12"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <Setter Property="Cursor" Value="Hand"/>
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border Background="{TemplateBinding Background}" 
                                           CornerRadius="10" 
                                           Padding="{TemplateBinding Padding}">
                                        <Border.Effect>
                                            <DropShadowEffect Color="#5E60CE" BlurRadius="8" ShadowDepth="0" Opacity="0.4"/>
                                        </Border.Effect>
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#6B6DCE"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#5E60CE" BlurRadius="12" ShadowDepth="0" Opacity="0.6"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </Grid>

        <!-- PDF Viewer Section -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="300"/>
            </Grid.ColumnDefinitions>

            <!-- PDF Display Panel -->
            <Border Grid.Column="0" Background="#1E2032" CornerRadius="20" Margin="0,0,20,0" Padding="25">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="90"/>
                    </Grid.RowDefinitions>

                    <!-- PDF Display Area -->
                    <Border Grid.Row="0" Background="White" CornerRadius="18" Margin="0,0,0,25">
                        <Border.Effect>
                            <DropShadowEffect Color="Black" BlurRadius="15" ShadowDepth="2" Opacity="0.5"/>
                        </Border.Effect>
                        <Grid>
                            <!-- WebView2 for PDF Display -->
                            <wv2:WebView2 x:Name="PdfWebView" Visibility="Collapsed"/>

                            <!-- No PDF Message -->
                            <StackPanel x:Name="NoPdfPanel" 
                                       VerticalAlignment="Center" 
                                       HorizontalAlignment="Center"
                                       Visibility="Visible">
                                <Border Background="#1E2032" CornerRadius="20" Padding="40,35" Margin="0,0,0,25">
                                    <StackPanel>
                                        <TextBlock Text="📄" FontSize="72" Foreground="#5E60CE" HorizontalAlignment="Center" Margin="0,0,0,20">
                                            <TextBlock.Effect>
                                                <DropShadowEffect Color="#5E60CE" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                                            </TextBlock.Effect>
                                        </TextBlock>
                                        <TextBlock Text="No PDF loaded" 
                                                  FontSize="20" 
                                                  FontWeight="SemiBold"
                                                  Foreground="White" 
                                                  HorizontalAlignment="Center"
                                                  Margin="0,0,0,8"/>
                                        <TextBlock Text="Click 'Open PDF' to start viewing" 
                                                  FontSize="14" 
                                                  Foreground="#8B8B9A" 
                                                  HorizontalAlignment="Center"/>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </Border>

                    <!-- Controls Panel -->
                    <Grid Grid.Row="1">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                            <!-- Previous Page Button -->
                            <Button x:Name="PrevButton" 
                                   Width="50" Height="50" 
                                   Background="Transparent" 
                                   BorderThickness="0" 
                                   Cursor="Hand" 
                                   Margin="0,0,20,0"
                                   Click="PrevPage_Click"
                                   IsEnabled="False">
                                <TextBlock Text="◀" FontSize="24" Foreground="White"/>
                            </Button>

                            <!-- Page Info -->
                            <Border Background="#252738" 
                                   CornerRadius="12" 
                                   Padding="25,12"
                                   Margin="0,0,20,0">
                                <Border.Effect>
                                    <DropShadowEffect Color="Black" BlurRadius="8" ShadowDepth="0" Opacity="0.2"/>
                                </Border.Effect>
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock Text="📄" FontSize="16" Margin="0,0,10,0" VerticalAlignment="Center"/>
                                    <TextBlock Text="Page " Foreground="#8B8B9A" FontSize="13" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="CurrentPageText" Text="1" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="2,0"/>
                                    <TextBlock Text=" / " Foreground="#8B8B9A" FontSize="13" VerticalAlignment="Center"/>
                                    <TextBlock x:Name="TotalPagesText" Text="?" Foreground="White" FontSize="14" FontWeight="Bold" VerticalAlignment="Center" Margin="2,0,0,0"/>
                                </StackPanel>
                            </Border>

                            <!-- Next Page Button -->
                            <Button x:Name="NextButton"
                                   Width="50" Height="50" 
                                   Background="Transparent" 
                                   BorderThickness="0" 
                                   Cursor="Hand"
                                   Click="NextPage_Click"
                                   IsEnabled="False">
                                <TextBlock Text="▶" FontSize="24" Foreground="White"/>
                            </Button>
                        </StackPanel>

                        <!-- Zoom Controls -->
                        <Border Background="#252738" CornerRadius="12" Padding="8,0" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,20,0">
                            <StackPanel Orientation="Horizontal">
                                <Button Content="−" 
                                       Width="38" Height="38"
                                       Background="Transparent"
                                       Foreground="White"
                                       BorderThickness="0"
                                       FontSize="22"
                                       FontWeight="Bold"
                                       Margin="0,0,8,0"
                                       Cursor="Hand"
                                       Click="ZoomOut_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#2A2D45"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                                <TextBlock x:Name="ZoomText" 
                                          Text="100%" 
                                          Foreground="White" 
                                          FontSize="13" 
                                          FontWeight="SemiBold"
                                          VerticalAlignment="Center"
                                          Margin="8,0"
                                          MinWidth="45"
                                          TextAlignment="Center"/>
                                <Button Content="+" 
                                       Width="38" Height="38"
                                       Background="Transparent"
                                       Foreground="White"
                                       BorderThickness="0"
                                       FontSize="22"
                                       FontWeight="Bold"
                                       Margin="8,0,0,0"
                                       Cursor="Hand"
                                       Click="ZoomIn_Click">
                                    <Button.Style>
                                        <Style TargetType="Button">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="#2A2D45"/>
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Button.Style>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>
                </Grid>
            </Border>

            <!-- Right Info Panel -->
            <Border Grid.Column="1" Background="#1E2032" CornerRadius="20" Padding="25">
                <Border.Effect>
                    <DropShadowEffect Color="Black" BlurRadius="20" ShadowDepth="0" Opacity="0.3"/>
                </Border.Effect>
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel>
                        <!-- Title -->
                        <Border Background="#252738" CornerRadius="12" Padding="15,12" Margin="0,0,0,25">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="ℹ️" FontSize="18" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Document Info" 
                                          Foreground="White" 
                                          FontWeight="Bold" 
                                          FontSize="16" 
                                          VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- File Name -->
                        <TextBlock Text="FILE NAME" Foreground="#8B8B9A" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <Border Background="#252738" CornerRadius="10" Padding="15,12" Margin="0,0,0,20">
                            <TextBlock x:Name="FileNameText" 
                                      Text="No file loaded" 
                                      Foreground="White" 
                                      FontSize="13" 
                                      TextWrapping="Wrap"
                                      LineHeight="20"/>
                        </Border>

                        <!-- File Size -->
                        <TextBlock Text="FILE SIZE" Foreground="#8B8B9A" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,8"/>
                        <Border Background="#252738" CornerRadius="10" Padding="15,12" Margin="0,0,0,25">
                            <TextBlock x:Name="FileSizeText" 
                                      Text="—" 
                                      Foreground="White" 
                                      FontSize="13"/>
                        </Border>

                        <!-- Settings -->
                        <Border Background="#252738" CornerRadius="12" Padding="15,12" Margin="0,0,0,20">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="⚙️" FontSize="16" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                <TextBlock Text="Settings" 
                                          Foreground="White" 
                                          FontWeight="SemiBold" 
                                          FontSize="14" 
                                          VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <Border Background="#252738" CornerRadius="10" Padding="15,12" Margin="0,0,0,25">
                            <Grid>
                                <TextBlock Text="Fit to Page" Foreground="#EAEAEA" VerticalAlignment="Center" FontSize="13"/>
                                <CheckBox HorizontalAlignment="Right" VerticalAlignment="Center" IsChecked="True">
                                    <CheckBox.Style>
                                        <Style TargetType="CheckBox">
                                            <Setter Property="Foreground" Value="#5E60CE"/>
                                        </Style>
                                    </CheckBox.Style>
                                </CheckBox>
                            </Grid>
                        </Border>

                        <!-- Recent Files -->
                        <TextBlock Text="Recent PDFs" 
                                  Foreground="White" 
                                  FontWeight="SemiBold" 
                                  FontSize="14" 
                                  Margin="0,0,0,15"/>

                        <Border Background="#252738" CornerRadius="10" Padding="15,12" Margin="0,0,0,10" Cursor="Hand">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2A2D45"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="📄 document1.pdf" Foreground="#EAEAEA" FontSize="13"/>
                        </Border>
                        <Border Background="#252738" CornerRadius="10" Padding="15,12" Margin="0,0,0,10" Cursor="Hand">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2A2D45"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="📄 tutorial.pdf" Foreground="#EAEAEA" FontSize="13"/>
                        </Border>
                        <Border Background="#252738" CornerRadius="10" Padding="15,12" Margin="0,0,0,0" Cursor="Hand">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Style.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter Property="Background" Value="#2A2D45"/>
                                        </Trigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <TextBlock Text="📄 guide.pdf" Foreground="#EAEAEA" FontSize="13"/>
                        </Border>
                    </StackPanel>
                </ScrollViewer>
            </Border>
        </Grid>
    </Grid>
</Page>
