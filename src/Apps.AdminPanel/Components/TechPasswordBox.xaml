<UserControl x:Class="Apps.AdminPanel.Components.TechPasswordBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             x:Name="ThisControl">

    <Grid>
        <!-- الأيقونة: تم تغيير اللون إلى الرمادي الهادئ -->
        <materialDesign:PackIcon Kind="{Binding Icon, ElementName=ThisControl}" 
                               Width="20" Height="20"
                               Foreground="{StaticResource TextSecondary}"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Left"
                               Margin="15,0,0,0"
                               Panel.ZIndex="1"/>

        <!-- حقل كلمة المرور -->
        <!-- 
             تم تحديث الألوان:
             Background -> ControlBackground
             Foreground -> TextPrimary
             BorderBrush -> BorderColor
             CornerRadius -> 4
             Height -> 45 (ليتطابق مع TechTextBox)
        -->
        <PasswordBox x:Name="InnerPasswordBox"
                     Style="{StaticResource MaterialDesignOutlinedPasswordBox}"
                     
                     materialDesign:HintAssist.Hint="{Binding Hint, ElementName=ThisControl}"
                     
                     Background="{StaticResource ControlBackground}"
                     Foreground="{StaticResource TextPrimary}"
                     BorderBrush="{StaticResource BorderColor}"
                     CaretBrush="{StaticResource AccentColor}"
                 
                     materialDesign:TextFieldAssist.UnderlineBrush="{StaticResource AccentColor}"
                     materialDesign:TextFieldAssist.TextFieldCornerRadius="4"
                     materialDesign:HintAssist.IsFloating="False"
                     
                     FontSize="14"
                     Height="45"
                     VerticalContentAlignment="Center"
                     Padding="45,0,10,0"
                     
                     PasswordChanged="InnerPasswordBox_PasswordChanged"/>
    </Grid>
</UserControl>