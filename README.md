# SecureGate DRM Solution

## 🚀 نظرة عامة على المشروع

**SecureGate DRM Solution** هو مشروع نظام حماية رقمية متكامل (Digital Rights Management - DRM) مصمم لتأمين المحتوى الرقمي (مثل ملفات الفيديو) ومنع الوصول غير المصرح به أو التوزيع غير القانوني. يعتمد النظام على مزيج من تقنيات .NET (C# WPF) لتطبيقات الواجهة والمستخدم، ونواة أمان أصلية (Native C++) لتنفيذ عمليات التشفير والتحقق الحساسة على مستوى النظام، لضمان أقصى درجات الأمان والأداء.

**الهدف الرئيسي:** توفير حل DRM قوي ومرن يمكنه حماية المحتوى، التحكم في الوصول بناءً على بصمة الجهاز، واكتشاف محاولات التلاعب بالبيئة (مثل الآلات الافتراضية).

---

## 🏗️ هيكلية المشروع

يعتمد هذا الحل على مبادئ **Clean Architecture** و **SOLID Principles** لضمان فصل الاهتمامات، قابلية الصيانة، وقابلية التوسع. تم تقسيم الحل إلى عدة مشاريع (Projects) رئيسية:

-   **`DRM.Security.Native` (C++ DLL):** نواة الأمان الأصلية التي تقوم بجميع العمليات الحساسة (استخراج البصمة، فحص الـ Anti-VM، التشفير/فك التشفير).
-   **`DRM.Shared` (C# Class Library):** مكتبة مشتركة تحتوي على تعريفات البيانات (مثل `CoreResponse`) وWrappers لربط C# بـ C++ Native.
-   **`Apps.ClientTool` (C# WPF):** تطبيق لسطح المكتب للمستخدم لاستخراج بصمة جهازه.
-   **`Apps.AdminPanel` (C# WPF):** تطبيق لسطح المكتب للمسؤول لإدارة المحتوى، تشفيره، وتوليد التراخيص.
-   **`Apps.SecurePlayer` (C# WPF):** مشغل وسائط آمن لتشغيل المحتوى المشفر مباشرة من الذاكرة.
-   **`tests/`:** مشاريع للاختبارات الوحدوية (Unit Tests) والاختبارات التكاملية (Integration Tests) لكل من C++ و C#.

---

## 🎯 الميزات الرئيسية

1.  **بصمة الجهاز الفريدة (Unique Device Fingerprinting):**
    * استخلاص معرفات العتاد الفريدة للجهاز (Motherboard Serial, CPU ID, HDD Serial).
    * توليد بصمة جهاز آمنة (SHA-256).
2.  **حماية المحتوى (Content Protection):**
    * تشفير وفك تشفير المحتوى باستخدام خوارزميات قوية (AES-256) ومفاتيح مشتقة من بصمة الجهاز (PBKDF2).
    * تخزين مفتاح الجلسة (Session Key) بشكل مشفر داخل الملف المحمي.
3.  **المشغل الآمن داخل الذاكرة (Secure In-Memory Player):**
    * تشغيل المحتوى المشفر مباشرة من الذاكرة دون الحاجة لفك تشفيره وتخزينه مؤقتاً على القرص.
4.  **مكافحة التلاعب بالبيئة (Anti-Tampering & Anti-VM):**
    * كشف البيئات الافتراضية (Virtual Machines) لمنع تشغيل المحتوى في بيئات غير آمنة.
    * التحقق من سلامة البيئة والتوقيت (Time Guard).
5.  **نظام تسجيل الأخطاء الموحد (Unified Error Logging):**
    * نظام `CoreResponse` و `NativeLogger` يوفر تفاصيل غنية عن نجاح أو فشل العمليات، بما في ذلك رسائل الخطأ والسجلات التفصيلية من نواة C++.

---

## 🚀 كيفية البدء (للمطورين الجدد)

1.  **الاستنساخ (Clone) المستودع:**
    ```bash
    git clone [https://github.com/munawwaer/SecureGate_DRM_Solution.git](https://github.com/munawwaer/SecureGate_DRM_Solution.git)
    cd SecureGate_DRM_Solution
    ```
2.  **فتح الحل في Visual Studio:**
    * افتح `SecureGate_DRM_Solution.sln` باستخدام Visual Studio 2022.
3.  **استعادة حزم NuGet:**
    * Visual Studio سيقوم تلقائياً باستعادة حزم NuGet المطلوبة لمشاريع C# (`Newtonsoft.Json` وغيرها).
4.  **البناء (Build):**
    * من قائمة `Build`، اختر `Rebuild Solution`. تأكد من أن جميع المشاريع تبنى بنجاح.
    * إذا واجهت مشاكل في بناء C++، تأكد من تثبيت Workload `Desktop development with C++` في Visual Studio.

---

#`

**قواعد العمل على Git:**

* اعمل دائماً على فرع (Branch) خاص بميزتك (مثلاً: `feature/fingerprint-generation`).
* اطلب مراجعة الكود (Pull Request) عند الانتهاء إلى الفرع الرئيسي `develop` (أو `main`).
* تجنب العمل مباشرة على فرعي `main` أو `develop`.

---

## 📄 الوثائق

جميع وثائق التصميم، تدفق العمل، ومواصفات API التفصيلية يمكن العثور عليها في مجلد `docs/`.

---

## 📞 الدعم والمساعدة
