# 🔐 شرح آلية التشفير وفك التشفير في مشروع SecureGate DRM

يوضح هذا المستند التفاصيل التقنية لكيفية عمل النظام الذي قمنا ببنائه، بدءاً من لوحة الإدارة (AdminPanel) وصولاً إلى المشغل الآمن (SecurePlayer).

---

## 🏗️ الهيكلية العامة (Architecture)
يعتمد المشروع على تصميم **هجين (Hybrid)** يجمع بين سهولة الواجهات في **C# (WPF)** وقوة الأداء والأمان في **C++**.

1.  **المحرك الأساسي (Core Engine - C++):**
    *   عبارة عن مكتبة `Native DLL` مكتوبة بلغة C++.
    *   تحتوي على خوارزميات التشفير (AES-256) والتعامل المباشر مع الذاكرة.
    *   الهدف: جعل كسر الحماية صعباً جداً لأن الكود الحساس غير مكتوب بـ C# القابل للعكس (Decompilation).

2.  **الطبقة الوسيطة (Wrapper - C#):**
    *   فئة `SecureVideoEngine.cs`.
    *   تعمل كجسر للتواصل مع مكتبة C++.
    *   تستخدم `DllImport` لاستدعاء الدوال الأصلية.

3.  **تطبيقات الواجهة:**
    *   **AdminPanel:** لتشفير الملفات.
    *   **SecurePlayer:** لفك تشفير وتشغيل الملفات.

---

## 🛡️ أولاً: عملية التشفير (في AdminPanel)

عندما تختار ملف فيديو وتضغط "تشفير"، تحدث الخطوات التالية:

### 1. توليد المفاتيح
*   يقوم النظام بتوليد **مفتاح تشفير (Key)** عشوائي بطول 32 بايت (256-bit).
*   يقوم بتوليد **متجه تهيئة (IV)** عشوائي بطول 16 بايت (لزيادة العشوائية).
*   يتم ذلك باستخدام `GenerateRandomBytes` من مكتبة C++ لضمان العشوائية الحقيقية.

### 2. معالجة الملف (Chunking)
*   لا يتم قراءة الملف كاملاً في الذاكرة (لتجنب استهلاك الرام مع الملفات الكبيرة).
*   يتم فتح الملف الأصلي وقراءته **قطعة قطعة (Chunks)**، حجم القطعة 1 ميجابايت.

### 3. التشفير الفعلي
*   يتم تمرير كل قطعة (Chunk) إلى المحرك `SecureVideoEngine`.
*   يقوم المحرك باستدعاء دالة C++:
    ```cpp
    ProcessFrame(context, data, length, iv, offset);
    ```
*   تقوم دالة C++ بتشفير البيانات في مكانها (In-Place Encryption) باستخدام خوارزمية **AES-256** في وضع **CTR Mode** (أو ما يماثله حسب التنفيذ الداخلي).

### 4. الحفظ
*   يتم كتابة البيانات المشفرة في ملف جديد بامتداد `.enc`.
*   يتم حفظ المفتاح والـ IV في ملف `.key` (هذا مؤقت للتجربة، لاحقاً سيحفظ في قاعدة بيانات).

---

## 🎬 ثانياً: عملية فك التشفير (في SecurePlayer)

عندما يفتح المستخدم ملف `.enc` في المشغل:

### 1. التحقق وجلب المفتاح
*   يبحث المشغل عن ملف `.key` الموافق للملف المشفر.
*   يقرأ المفتاح والـ IV.

### 2. فك التشفير (Decryption)
*   يستخدم المشغل خدمة `DecryptionService`.
*   بما أننا نستخدم خوارزمية متناظرة (Symmetric Algorithm)، فإن عملية فك التشفير هي نفسها عملية التشفير رياضياً (XOR).
*   يتم تمرير البيانات المشفرة والمفتاح إلى نفس محرك C++.
*   يعيد المحرك البيانات إلى أصلها (فيديو قابل للتشغيل).

### 3. التشغيل
*   حالياً في النسخة التجريبية: نقوم بحفظ الفيديو المفكوك في ملف مؤقت (Temp File) وتشغيله.
*   **في النسخة النهائية (الأكثر أماناً):** سيتم فك التشفير مباشرة من الذاكرة إلى مشغل الفيديو (Memory Stream) دون الكتابة على القرص الصلب، لمنع المستخدم من نسخ الملف.

---

## 🎨 ثالثاً: الواجهة والتعريب

*   تم تصميم الواجهات بـ **WPF/XAML** بتصميم عصري (Dark Theme).
*   تم تعريب `SecurePlayer` بالكامل عن طريق:
    *   تغيير النصوص (Strings) إلى العربية.
    *   تغيير اتجاه التخطيط `FlowDirection="RightToLeft"` لضمان ظهور القوائم والنصوص بشكل صحيح من اليمين.

---

## 🚀 ملخص تقني

| المكون | التقنية المستخدمة | الوظيفة |
| :--- | :--- | :--- |
| **لغة البرمجة (الواجهة)** | C# (.NET 9.0) | بناء النوافذ وإدارة الملفات |
| **لغة البرمجة (النواة)** | C++ (Native) | التشفير، الحماية، بصمة الجهاز |
| **الخوارزمية** | AES-256 | المعيار العالمي للتشفير القوي |
| **طريقة الربط** | P/Invoke (DllImport) | التواصل بين C# و C++ |
